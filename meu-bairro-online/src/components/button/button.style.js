import { COLORS, FONT_SIZE } from "../../constants/themes";

export const styles = {
    btn: {
        width: "100%",
        backgroundColor: COLORS.red
    },
    texto: {
        fontSize: FONT_SIZE.md,
        color: "white",
        padding: 14,
        textAlign: "center"
    }
}